# SERPsmith demo site

Disposable fixture for the bundled Markdown adapter. Copy this directory to a temporary location, initialize Git with the runtime's approved Git capability, replace the profile repository/state paths, commit the baseline, and run preflight. Never connect this fixture to a public remote.

The fixture contains two existing Markdown posts, a simple XML sitemap, and an image directory. It intentionally has no deployment service or credentials.