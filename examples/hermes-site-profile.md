# Hermes profile example

Use `generic-git-site-profile.json`; it is valid under `serpsmith-core-v25`. Complete the runtime capability map. Hermes remains experimental until fixture, resume, failure injection, search adapters, scheduling, action boundary, and final delivery pass.
