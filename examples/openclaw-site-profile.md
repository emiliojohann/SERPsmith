# OpenClaw profile example

Use `generic-git-site-profile.json`; it is valid under `serpsmith-core-v27`. Do not create a vendor-specific schema. Complete the runtime capability map and keep tool names, destinations, credentials, and scheduler IDs outside the distributable profile.
